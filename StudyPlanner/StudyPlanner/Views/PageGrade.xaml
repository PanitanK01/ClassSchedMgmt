<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:SP="clr-namespace:StudyPlanner.Controls"
             x:Class="StudyPlanner.Views.PageGrade" 
             BackgroundColor="{DynamicResource BackgroundColor}">
    
   
        <StackLayout>
            <ScrollView Padding="{DynamicResource PagePadding}" >
                <StackLayout Spacing="{DynamicResource StackSpacing}">

                <SP:Header Text="Grade" />
                <SP:Input x:Name="ip_CGPAPicker" Type="Picker" Header="GPA / CGPA:" SelectedItem="{Binding Grade.CGPA}"/>

            </StackLayout>
            </ScrollView>
        </StackLayout>
    




</ContentPage>